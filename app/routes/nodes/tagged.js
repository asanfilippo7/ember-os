import Ember from 'ember';

export default Ember.Route.extend({
//    model: function() {
//        return this.store.filter('node', function(node) {
//            if(node.get('tags').length > 0) {
//                return node;
//            }
//        });
//    },
//    renderTemplate(controller, model) {
//        this.render('nodes.tagged', { model: model });
//    }
});
