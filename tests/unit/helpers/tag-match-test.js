import { tagMatch } from '../../../helpers/tag-match';
import { module, test } from 'qunit';

module('Unit | Helper | tag match');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tagMatch(42);
  assert.ok(result);
});
